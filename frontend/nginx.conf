server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index home.html;

    location / {
        try_files $uri $uri/ /home.html;
    }

    location /home.html {
        try_files $uri =404;
    }

    location /status.html {
        try_files $uri =404;
    }
}
